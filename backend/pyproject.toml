[project]
name = "print-job-manager-backend"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "fastapi==0.116.1",
    "uvicorn==0.35.0",
]

[dependency-groups]
test = [
    "pytest==8.4.1",
    "httpx==0.28.1",
]
